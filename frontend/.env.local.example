# Where the FastAPI backend lives. Locally this is the uvicorn port; on Vercel
# set it to the Render URL (no trailing slash) in Project Settings -> Environment
# Variables, and add that same Vercel URL to ALLOWED_ORIGINS on Render.
NEXT_PUBLIC_API_BASE=http://localhost:8000
