# python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# secrets and local state
.env
.env.local
*.sqlite
*.sqlite3
*.sqlite-shm
*.sqlite-wal
traceci_checkpoints.sqlite

# node / next
node_modules/
.next/
out/
.vercel/
next-env.d.ts.tmp

# eval runs are timestamped artifacts of a live model call, not source.
# The harness and its expectations are versioned; the numbers it produced on
# one afternoon against one provider quota are not.
backend/evals/results/

# os
.DS_Store
Thumbs.db
