# --- agent ---------------------------------------------------------------
langchain>=0.3.0
langchain-core>=0.3.0
langgraph>=0.2.40
langgraph-checkpoint-sqlite>=2.0.0
aiosqlite>=0.20.0

# --- providers (BYOK: installed, but never given a key from the environment)
# groq first: it is the free one, and therefore the default.
langchain-groq>=0.2.0
langchain-openai>=0.2.0
langchain-anthropic>=0.2.0
langchain-google-genai>=2.0.0

# --- api -----------------------------------------------------------------
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0
pydantic>=2.8.0
python-dotenv>=1.0.0
